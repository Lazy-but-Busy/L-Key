# Public configuration only. Everything here reaches the browser bundle —
# never put a secret in a NEXT_PUBLIC_ variable (CLAUDE.md §22).
NEXT_PUBLIC_APP_ENV=local
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
