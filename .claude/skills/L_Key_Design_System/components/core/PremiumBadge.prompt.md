The Pro marker. `PRO` in mono bold on Guitar Orange with a 2px ring — the only premium signal in the system.

```jsx
<PremiumBadge />
<PremiumBadge size="sm" tone="inverse">PRO</PremiumBadge>
```

Never pair with gold, gradients or glow (DESIGN.md §32, §69). Premium communicates *more power*, not luxury.
