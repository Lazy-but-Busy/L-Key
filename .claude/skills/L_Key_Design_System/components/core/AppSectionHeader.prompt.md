Separates screen sections. Titles are always uppercase; the optional action is mono with the file's 9.3px arrow.

```jsx
<AppSectionHeader title="Recent Riffs" actionLabel="View library" />
<AppSectionHeader size="lg" title="Song Database" />
```
