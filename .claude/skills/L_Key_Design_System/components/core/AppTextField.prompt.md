The only text input in the system: 45.59px tall, white, 1px inset ring plus a 4px hard shadow, JetBrains Mono 14 (DESIGN.md §16 — no floating labels).

```jsx
<AppTextField label="Title" placeholder="Search ID, Title, Artist..." icon={<img src="assets/icons/search.svg" width={18} height={18} alt="" />} />
```

Placeholder colour is `--lk-grey-400`. Labels are uppercase mono 12; put them above the field, never inside it.
