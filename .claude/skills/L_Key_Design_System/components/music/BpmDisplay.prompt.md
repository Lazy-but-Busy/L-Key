Tempo readout for metronome, practice and song screens.

```jsx
<BpmDisplay bpm={120} beats={4} activeBeat={0} timeSignature="4/4" />
```

Beat one is larger and fills Guitar Orange when sounding; the other beats fill black. Rings stay visible when a beat is off so the bar length is always readable.
