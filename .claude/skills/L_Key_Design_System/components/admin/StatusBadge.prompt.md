Status in admin tables and payment views.

```jsx
<StatusBadge status="published" />
<StatusBadge status="draft" />
<StatusBadge status="failed">Payment failed</StatusBadge>
```

Always dot + word (DESIGN.md §56 — never rely on colour alone). Published green comes straight from the file (`#E6FFED` / `#28A745`).
